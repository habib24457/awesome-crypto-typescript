export interface ICrypto {
  exchangeId: string;
  exchangeUrl: string;
  name: string;
  percentTotalVolume: string;
  rank: string;
  socket: boolean;
  tradingPairs: string;
  updated: string;
  volumeUsd: string;
}
